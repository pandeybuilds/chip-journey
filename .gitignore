# build artifacts
*.o
*.out
a.out
target/
.DS_Store
# compiled C programs (the source files are kept, the binaries are not)
base_convert
bool_eval
to_binary
# IDE and build folders (CLion / JetBrains)
.idea/
cmake-build-*/
# anki media
*.apkg
